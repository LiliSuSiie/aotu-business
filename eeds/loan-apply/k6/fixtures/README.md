# 本地证件图片

`MODE=full` 会读取本目录下的两个文件并调用测试环境真实上传接口：

- `id-front.jpg`：与本次测试客户一致的身份证正面；
- `id-back.jpg`：与本次测试客户一致的身份证反面。

两张图片由测试人员放在本机，已被 `.gitignore` 排除。不要使用 `进件.json` 中的 Base64 图片，不要提交包含真实身份证信息的文件。若 OCR 挡板不校验图片内容，仍建议使用经脱敏、已获授权的测试证件图，以便核验上传和文件关联。
