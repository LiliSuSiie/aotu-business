# Aotu Business — 测试自动化项目记忆规范

本文件为本目录及其子目录内所有需求分析、方案设计、代码生成、测试实施与验收工作的长期约束。除非用户明确要求例外，后续工作必须遵循。

## 核心目标

交付的瓶颈不在于生成测试代码，而在于把不清晰的需求转成可验收、可追溯、可复用的测试设计，并以低成本证明覆盖充分且功能真实通过。

每项工作优先产出：

- 明确的业务规则、前置条件、关键数据组合、预期结果与验收边界；
- 可确定性执行的自动化资产；
- 能定位失败原因并支持复验的证据。

需求含糊、规则冲突或验收口径缺失时，先显式记录待澄清项、假设和风险；不得仅凭生成脚本掩盖设计不确定性。

## 架构原则：AI 生成，确定性执行

- AI 用于理解业务、抽取语义、生成与修订可沉淀的代码或结构化脚本。
- 实际回归执行优先交给确定性框架与固定脚本，例如 k6、Playwright、Selenium 或项目自定义执行器。
- 不将实时视觉推理或实时 AI UI 操作作为高频回归的默认执行路径；其成本、速度和稳定性不适合工业化闭环。
- 可以用 Go 开发任何需要的 k6 扩展；AI 生成的代码必须按既定格式沉淀并可由人工或 AI 重复运行。

## 自动化优先级

1. 接口自动化为默认和最高优先级，优先覆盖主业务流、关键参数组合、数据依赖与核心断言。
2. UI 自动化仅补足接口难以验证的关键交互、页面状态和端到端链路。
3. 无文档或复杂内部系统，优先通过浏览器录制获得真实请求—响应序列；AI 负责解析依赖、过滤噪声并合成可执行接口场景（优先 k6）。
4. 以快速形成高覆盖回归网为导向；常见单场景的录制到可运行脚本，目标是约 5 分钟内完成。

## UI 自动化规范

- 避免以易碎的 XPath/CSS 硬编码作为唯一定位策略。
- 优先从 UI 树、可访问性信息或截图中抽取元素语义与布局，沉淀为结构化 JSON 操作流。
- 执行时先进行快速、确定性的结构校验与日志断言。
- 仅在失败、歧义或关键验收节点按需调用多模态视觉校验；不得把视觉模型逐步实时驱动作为常规路径。

## 业务知识库与资产沉淀

- 不假定需求文档始终完整可靠；以经过人工确认的录制流量、JSON 脚本和测试代码作为业务知识的高可信快照。
- 每个可复用场景应尽可能保留接口参数组合、上下游数据依赖、断言规则、环境前提和人工确认记录。
- 生成新用例时，优先检索并作为 Few-Shot 示例复用这些已确认资产，而非依赖冗长、可能过期的文字描述。

## 验收、归因与闭环

- 不只报告“脚本通过/失败”；验收输出应说明覆盖范围、关键断言、证据、未覆盖风险与结论。
- 测试失败时，收集失败日志、请求与响应报文、断言上下文、环境与版本信息，辅助判断为系统缺陷、环境问题、数据问题或脚本/断言过期。
- 若判断为脚本或断言过期，可由 AI 提出或完成脚本修正，并必须重跑验证；系统缺陷应保留可复现证据而非自动掩盖。
- 不含深度压测时，目标是将“测试覆盖 → 发现问题 → 修复脚本/功能 → 复验验收”压缩到 1 小时内完成。

## 质量门槛

新建或修改自动化资产前，确认需求口径和验收点；完成后至少验证：可重复执行、断言可解释、失败可归因、资产可复用。若任一项不成立，明确标注限制与下一步，而非宣称完成。
